# Terraform state records every resource attribute in plaintext. Never commit it.
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log
*.tfplan

*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl IS committed -- it pins provider checksums.
